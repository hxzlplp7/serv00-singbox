# Serv00/Hostuno Sing-box 脚本项目分析报告

## 📊 项目概览

### 分析的两个项目

| 项目 | 作者 | 主要特点 |
|------|------|----------|
| **sing-box-yg-main** | 甬哥 (yonggekkk) | 功能全面、界面友好、支持多功能主页 |
| **Sing-box-main** | 老王 (eooce) | 简洁高效、自动保活、支持环境变量 |

---

## 📁 项目结构对比

### 甬哥项目 (sing-box-yg-main)

```
sing-box-yg-main/
├── sb.sh              # VPS专用脚本 (168KB, 5258行)
├── serv00.sh          # Serv00/Hostuno专用脚本 (58KB, 1635行)
├── serv00keep.sh      # 保活脚本
├── kp.sh              # 辅助脚本
├── app.js             # Node.js 多功能主页
├── index.html         # 静态主页
├── README.md          # 项目说明
├── version/sversion   # 版本信息
├── workers_keep.js    # Cloudflare Workers 保活
├── SSH.yml            # GitHub Actions SSH
├── serv00.yml         # GitHub Actions 自动执行
└── 客户端配置说明.txt
```

### 老王项目 (Sing-box-main)

```
Sing-box-main/
├── sing-box.sh        # VPS专用脚本 (58KB)
├── sb_serv00.sh       # Serv00交互式脚本 (29KB, 792行)
├── sb_00.sh           # Serv00简化脚本
├── sb4.sh             # 无交互式安装脚本
├── README.md          # 项目说明
├── go/java/nodejs/php/python/  # 多环境支持
└── LICENSE
```

---

## 🔧 功能对比

### 支持的协议

| 协议 | 甬哥脚本 | 老王脚本 | 说明 |
|------|:--------:|:--------:|------|
| VLESS-Reality | ✅ | ✅ | Reality 协议 |
| VMess-WS | ✅ | ✅ | WebSocket 传输 |
| VMess-WS-TLS | ✅ | ✅ | TLS 加密 |
| Hysteria2 | ✅ | ✅ | QUIC 协议 |
| TUIC v5 | ❌ | ✅ | UDP 转发 |
| Argo 临时隧道 | ✅ | ✅ | 无需域名 |
| Argo 固定隧道 | ✅ | ✅ | 需要 Token |
| Shadowsocks | ❌ | ❌ | 未支持 |
| Trojan | ❌ | ❌ | 未支持 |

### 特色功能

| 功能 | 甬哥脚本 | 老王脚本 |
|------|:--------:|:--------:|
| 多功能网页主页 | ✅ | ✅ |
| 自动保活服务 | ✅ | ✅ |
| ProxyIP 功能 | ✅ | ❌ |
| 非标端口反代IP | ✅ | ❌ |
| 哪吒探针 v0 | ✅ | ✅ |
| 哪吒探针 v1 | ❌ | ✅ |
| TG 通知 | ❌ | ✅ |
| 订阅自动上传 | ❌ | ✅ |
| 环境变量配置 | 部分 | ✅ |
| 22个节点输出 | ✅ | ❌ |
| 3个IP全覆盖 | ✅ | ❌ |
| Clash-Meta 配置 | ✅ | ❌ |
| Sing-box 客户端配置 | ✅ | ❌ |

---

## 💻 代码分析

### 甬哥脚本 (serv00.sh) 核心结构

```bash
# 颜色定义
re="\033[0m"
red="\033[1;91m"
green="\e[1;32m"
yellow="\e[1;33m"
purple="\e[1;35m"

# 主要函数
├── read_ip()              # 读取IP选择
├── read_uuid()            # 读取UUID
├── read_reym()            # 读取Reality域名
├── check_port()           # 端口检查与配置
├── install_singbox()      # 主安装函数
├── uninstall_singbox()    # 卸载函数
├── argo_configure()       # Argo配置
├── download_and_run_singbox()  # 下载运行
├── get_links()            # 生成节点链接
├── servkeep()             # 保活服务安装
├── resservsb()            # 重启主进程
├── resargo()              # 重置Argo
├── showlist()             # 显示节点列表
├── showsbclash()          # 显示配置文件
├── resallport()           # 重置端口
└── menu()                 # 主菜单
```

### 老王脚本 (sb_serv00.sh) 核心结构

```bash
# 环境变量支持
export UUID=${UUID:-$(uuidgen -r)}
export ARGO_DOMAIN=${ARGO_DOMAIN:-''}
export ARGO_AUTH=${ARGO_AUTH:-''}
export NEZHA_SERVER=${NEZHA_SERVER:-''}

# 主要函数
├── check_port()           # 端口检查配置
├── check_website()        # 网站配置
├── read_variables()       # 读取哪吒/TG变量
├── install_singbox()      # 主安装函数
├── uninstall_singbox()    # 卸载函数
├── argo_configure()       # Argo配置
├── generate_config()      # 生成配置文件
├── download_singbox()     # 下载二进制
├── get_links()            # 生成节点链接
├── install_keepalive()    # 安装保活
├── quick_command()        # 快捷命令
└── menu()                 # 主菜单
```

---

## 📝 关键代码片段

### 端口检查逻辑 (甬哥版)

```bash
check_port () {
    port_list=$(devil port list)
    tcp_ports=$(echo "$port_list" | grep -c "tcp")
    udp_ports=$(echo "$port_list" | grep -c "udp")
    
    # 需要 2个TCP + 1个UDP
    if [[ $tcp_ports -ne 2 || $udp_ports -ne 1 ]]; then
        red "端口数量不符合要求，正在调整..."
        # 自动添加/删除端口
    fi
    
    export vless_port=$tcp_port1
    export vmess_port=$tcp_port2
    export hy2_port=$udp_port
}
```

### 端口检查逻辑 (老王版)

```bash
check_port () {
    port_list=$(devil port list)
    tcp_ports=$(echo "$port_list" | grep -c "tcp")
    udp_ports=$(echo "$port_list" | grep -c "udp")
    
    # 需要 1个TCP + 2个UDP
    if [[ $tcp_ports -ne 1 || $udp_ports -ne 2 ]]; then
        red "端口规则不符合要求，正在调整..."
        # 自动添加/删除端口
    fi
    
    export VMESS_PORT=$tcp_port
    export TUIC_PORT=$udp_port1
    export HY2_PORT=$udp_port2
}
```

### 配置文件生成 (sing-box config.json)

```json
{
  "log": {"disabled": true, "level": "info", "timestamp": true},
  "inbounds": [
    {
      "type": "hysteria2",
      "listen": "IP",
      "listen_port": PORT,
      "users": [{"password": "UUID"}],
      "tls": {
        "enabled": true,
        "alpn": ["h3"],
        "certificate_path": "cert.pem",
        "key_path": "private.key"
      }
    },
    {
      "type": "vless",
      "listen_port": PORT,
      "users": [{"uuid": "UUID", "flow": "xtls-rprx-vision"}],
      "tls": {
        "enabled": true,
        "server_name": "domain",
        "reality": {
          "enabled": true,
          "private_key": "KEY",
          "short_id": [""]
        }
      }
    },
    {
      "type": "vmess",
      "listen_port": PORT,
      "users": [{"uuid": "UUID"}],
      "transport": {"type": "ws", "path": "/path"}
    }
  ],
  "outbounds": [
    {"type": "direct", "tag": "direct"},
    {"type": "block", "tag": "block"}
  ]
}
```

---

## 🔄 节点链接生成

### VLESS-Reality 格式

```
vless://UUID@IP:PORT?encryption=none&flow=xtls-rprx-vision&security=reality&sni=DOMAIN&fp=chrome&pbk=PUBLIC_KEY&type=tcp&headerType=none#NAME
```

### VMess-WS 格式 (Base64)

```json
{
  "v": "2",
  "ps": "节点名称",
  "add": "IP或域名",
  "port": "端口",
  "id": "UUID",
  "aid": "0",
  "scy": "auto",
  "net": "ws",
  "type": "none",
  "host": "域名",
  "path": "/路径",
  "tls": "tls",
  "sni": "域名"
}
```

### Hysteria2 格式

```
hysteria2://PASSWORD@IP:PORT?security=tls&sni=www.bing.com&alpn=h3&insecure=1#NAME
```

### TUIC v5 格式

```
tuic://UUID:PASSWORD@IP:PORT?sni=www.bing.com&congestion_control=bbr&udp_relay_mode=native&alpn=h3&allow_insecure=1#NAME
```

---

## 📈 优化建议

### 综合脚本设计

基于两个项目的分析，新脚本应该：

1. **协议支持**
   - ✅ VLESS-Reality (甬哥版支持ProxyIP)
   - ✅ VMess-WS (支持TLS/无TLS)
   - ✅ Hysteria2
   - ✅ TUIC v5 (老王版特色)
   - ➕ Trojan-WS (新增)
   - ➕ Shadowsocks-2022 (新增)
   - ✅ Argo 隧道

2. **环境变量支持** (老王版优势)
   - 完整的环境变量配置
   - 支持无交互式安装

3. **用户体验** (甬哥版优势)
   - 详细的节点输出
   - 多客户端配置生成
   - ProxyIP 功能说明

4. **保活机制**
   - Node.js 多功能主页
   - Workers 保活
   - GitHub Actions 保活

5. **端口策略**
   - 2个TCP (VMess/VLESS)
   - 2个UDP (Hy2/TUIC)

---

## 🏁 总结

| 维度 | 甬哥脚本 | 老王脚本 | 推荐 |
|------|----------|----------|------|
| 协议数量 | 3个 | 4个 | 老王 |
| 功能丰富度 | ★★★★★ | ★★★★☆ | 甬哥 |
| 代码简洁性 | ★★★☆☆ | ★★★★★ | 老王 |
| 用户体验 | ★★★★★ | ★★★★☆ | 甬哥 |
| 自动化程度 | ★★★★☆ | ★★★★★ | 老王 |
| 文档完善度 | ★★★★★ | ★★★★☆ | 甬哥 |

**综合建议**: 新脚本应结合两者优点，采用甬哥的界面风格和详细输出，同时借鉴老王的环境变量支持和简洁代码结构，并添加更多协议支持。
